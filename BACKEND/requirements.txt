# Aegis — Vendor Risk Assessment
# Python 3.12+  |  install: pip install -r requirements.txt

# Vector store
chromadb>=0.6.0
pysqlite3-binary

# GPU inference engine (llama.cpp OpenAI-compatible server — run_gpu.py)
llama-cpp-python[server]>=0.3.0

# Model download helper (used by run_gpu.py's setup hint)
huggingface_hub>=0.24.0

# Web API (api.py) — FastAPI server + multipart for file uploads
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Text splitting
langchain-text-splitters>=0.3.0

# PDF parsing
PyMuPDF>=1.24.0

# Excel parsing and report generation
openpyxl>=3.1.0

# PDF report generation
reportlab>=4.2.0

# Data handling (benchmark history tables / dataframes)
pandas>=2.2.0

# Progress bars (CLI benchmark output)
tqdm>=4.66.0

# LLM client used ONLY by the standalone benchmark tool (benchmark.py)
ollama>=0.4.0
